name: "environment-config"
version: "1.0.0"
status: "ready"
description: "Environment and configuration management covering env var strategy, secret management, configuration layers, validation, environment parity, .env file management, feature flags, and anti-patterns"
context_cost: "small"

tags: [environment, configuration, secrets, env-vars, feature-flags, t3-env, dotenv, config-validation, environment-parity]

dependencies: []

superseded_by: ""

source_path: "source/SKILL.md"
summary_path: "summary.md"

triggers: [new-project-setup, config-refactor, secret-management, environment-setup, feature-flag-design, change-plan-with-config-changes]

created: "2026-03-27"
updated: "2026-03-27"

notes: "Covers all configuration layers from defaults to runtime overrides. Integrates with change_plan and regression_checklist for env-sensitive areas."
