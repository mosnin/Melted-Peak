---
name: validate
description: Run the validation plan and regression checklist after completing changes — requires running actual commands and confirming output
user-invocable: true
---

# /validate — Validation

1. Read `active/validation_plan.md` — run each verification step
2. Read `active/regression_checklist.md` — check sensitive areas affected by recent changes
3. For each check: run the actual command, read output, report pass/fail with evidence

Do NOT claim anything passes without running it. See `skills/verification-before-completion/`.
