# Framework Manifest Template
# Copy this file to frameworks/[name]/manifest.yaml

name: ""                    # Unique identifier (lowercase, hyphens)
version: "1.0.0"            # Semantic versioning
status: "draft"             # draft | ready | deprecated | superseded
description: ""             # One-line summary
context_cost: "medium"      # small | medium | large (context window impact)

tags: []                    # Searchable tags, e.g. [testing, api, architecture]

dependencies: []            # Other components required, e.g. [skills/debugging]

superseded_by: ""           # If status is superseded, name of replacement

source_path: "source/"      # Path to full documentation
summary_path: "summary.md"  # Path to operational overview

created: ""                 # YYYY-MM-DD
updated: ""                 # YYYY-MM-DD

notes: ""                   # Optional free-text notes
